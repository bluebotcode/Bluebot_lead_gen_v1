# Copy this file to .env and fill in a real key.
# Required: Google Cloud project with the Places API enabled and billing set up.
GOOGLE_PLACES_API_KEY=

# Optional: port the server listens on (defaults to 3000).
PORT=3000
